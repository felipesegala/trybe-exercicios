let saldo = 1000;

const soma = (saldo) => {
        return saldo;
    }
    console.log(saldo + 150);

const sub = (retira) => {
        return retira;
}
console.log(saldo - 50);

const mult = (x) => {
        return x;
}
console.log(saldo * 3);

const div = (divisao) => {
        return divisao;
}
console.log(saldo / 2);